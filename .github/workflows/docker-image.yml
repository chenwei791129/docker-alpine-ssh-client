name: Docker Image CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-image-name:$(date +%s)
    - name: Docker Login
      env: 
        DOCKER_USER: ${{secret.DOCKER_USER}}
        DOCKER_TOKEN: ${{secret.DOCKER_TOKEN}}
      run: docker login -u ${DOCKER_USER} -p ${DOCKER_TOKEN}
    #- name: Docker Push
    #  run: docker push ${{secret.DOCKER_USER}}/alpine-ssh-client
